# 🚀 Деплой на Render.com (Hosting)

## 📋 Важно: Telegram бот ≠ Web Service!

Telegram бот работает через **polling** (опрашивает Telegram API), не через HTTP порт.  
На Render.com используйте **Background Worker**, не Web Service!

---

## ✅ Правильная настройка на Render.com

### Шаг 1: Удалить старый Web Service (если создали)
1. Зайдите на [Render Dashboard](https://dashboard.render.com)
2. Найдите ваш сервис
3. Settings → Delete Service

### Шаг 2: Создать Background Worker
1. Click **"New"** → **"Background Worker"**
2. Connect GitHub repository: `sardorismatullaev707-collab/suprt`
3. Настройте:
   ```
   Name:           suprt-telegram-bot
   Branch:         main
   Root Directory: (оставить пустым)
   Runtime:        Node
   Build Command:  npm install
   Start Command:  npx tsx telegram-bot.ts
   ```

### Шаг 3: Добавить Environment Variables
Click "Advanced" → "Add Environment Variable":

```env
TELEGRAM_BOT_TOKEN=ваш_токен_бота
DEEPSEEK_API_KEY=sk-9e53766bf7e24d1ea27433d6294cb509
GOOGLE_SHEET_ID=ваш_sheet_id
GOOGLE_SERVICE_ACCOUNT_EMAIL=your-service-account@project.iam.gserviceaccount.com
GOOGLE_SERVICE_ACCOUNT_PRIVATE_KEY=-----BEGIN PRIVATE KEY-----\nYOUR_KEY\n-----END PRIVATE KEY-----\n
```

⚠️ **Важно:** Для `GOOGLE_SERVICE_ACCOUNT_PRIVATE_KEY` используйте `\n` для переносов строк!

### Шаг 4: Deploy
Click **"Create Background Worker"**

---

## 📊 Мониторинг

После деплоя можно смотреть логи:
- Dashboard → Ваш Worker → Logs

Вы должны увидеть:
```
[✓] Loaded 20 knowledge base entries
[✓] Telegram Bot started!
```

---

## 🔧 Альтернатива: Использовать render.yaml (Blueprint)

Если хотите автоматический деплой через `render.yaml`:

1. В GitHub repo уже есть файл `render.yaml`
2. На Render.com: New → Blueprint → Connect repository
3. Render автоматически создаст Background Worker по конфигурации

---

## 💰 Стоимость

**Free Tier (Бесплатно):**
- ✅ 750 часов в месяц
- ✅ Достаточно для 1 бота 24/7
- ⚠️ Засыпает после 15 минут неактивности (не подходит!)

**Starter ($7/месяц):**
- ✅ Работает 24/7 без перерывов
- ✅ Не засыпает
- ✅ Рекомендуется для продакшена

---

## 🚨 Проблема: "Port scan timeout"

Если видите ошибку:
```
Port scan timeout reached, no open ports detected
```

**Решение:** Вы создали Web Service вместо Background Worker!
- Web Service ожидает HTTP порт
- Telegram бот не использует HTTP
- Используйте **Background Worker**!

---

## 🎯 Альтернативные варианты хостинга:

### 1. Heroku
- Поддерживает Node.js
- Есть бесплатный tier (но ограниченный)
- Нужен `Procfile`: `worker: npx tsx telegram-bot.ts`

### 2. Railway.app
- Проще чем Render
- $5/месяц
- Автоматический деплой из GitHub

### 3. DigitalOcean App Platform
- $5/месяц
- Стабильный хостинг

### 4. VPS (DigitalOcean Droplet, AWS EC2)
- $5-10/месяц
- Полный контроль
- Нужно настроить самостоятельно

### 5. Локальный сервер
- Бесплатно
- Держать компьютер включенным 24/7
- Не рекомендуется для продакшена

---

## ✅ Рекомендация для 30 клиентов:

**Вариант 1: Railway.app** (Проще)
- $5/месяц за бота
- 30 клиентов = 30 ботов = $150/месяц
- MRR: $2,250 - $150 = $2,100 прибыль

**Вариант 2: VPS сервер** (Дешевле)
- DigitalOcean Droplet $20/месяц (2 GB RAM)
- Можно запустить все 30 ботов на одном сервере
- MRR: $2,250 - $20 = $2,230 прибыль

---

## 📝 Для клиентов

Когда настраиваете бота для клиента, просто:
1. Создайте новый Background Worker на Render
2. Добавьте environment variables клиента
3. Deploy
4. Дайте клиенту URL логов для мониторинга

---

## 🆘 Troubleshooting

### Бот не отвечает после деплоя
```bash
# Проверьте логи на Render Dashboard
# Убедитесь что все ENV variables установлены
```

### Ошибка "Invalid API key"
```bash
# Проверьте DEEPSEEK_API_KEY в Environment Variables
```

### Ошибка Google Sheets
```bash
# Проверьте что Service Account имеет доступ к таблице
# Проверьте формат GOOGLE_SERVICE_ACCOUNT_PRIVATE_KEY (с \n)
```

---

**Успешного деплоя! 🚀**

