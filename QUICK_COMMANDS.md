# 🚀 Быстрые команды для работы с проектом

## 📋 Ежедневные команды

### Запуск бота
```bash
cd "/Users/apple/VS projects/suprt local"
npx tsx telegram-bot.ts
```

### Остановка бота
```bash
pkill -f "tsx"
```

### Перезапуск бота
```bash
pkill -f "tsx" && npx tsx telegram-bot.ts
```

---

## 🔄 Git команды

### Сохранить изменения
```bash
cd "/Users/apple/VS projects/suprt local"
git add .
git commit -m "Описание изменений"
git push origin main
```

### Проверить статус
```bash
git status
```

### Посмотреть историю
```bash
git log --oneline -10
```

---

## 👥 Настройка нового клиента

### 1. Создать копию проекта
```bash
cd "/Users/apple/VS projects"
cp -r "suprt local" "client-name"
cd "client-name"
```

### 2. Очистить старые данные
```bash
rm -rf baileys_auth/
rm .env
```

### 3. Создать новый .env
```bash
nano .env
```

Вставить:
```env
TELEGRAM_BOT_TOKEN=новый_токен_клиента
DEEPSEEK_API_KEY=sk-9e53766bf7e24d1ea27433d6294cb509
GOOGLE_SHEET_ID=id_таблицы_клиента
GOOGLE_SERVICE_ACCOUNT_EMAIL=email_service_account
GOOGLE_SERVICE_ACCOUNT_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
```

### 4. Изменить промпт AI
```bash
nano ai.ts
```

Найти строку ~93 и изменить название компании:
```typescript
content: `You are a friendly assistant for [НАЗВАНИЕ КЛИЕНТА]...
```

### 5. Запустить бота
```bash
npx tsx telegram-bot.ts
```

---

## 📊 Мониторинг

### Проверить работает ли бот
```bash
ps aux | grep tsx
```

### Посмотреть логи (если запущен в фоне)
```bash
tail -f logs.jsonl
```

---

## 🔧 Troubleshooting

### Бот не отвечает
```bash
# 1. Остановить все процессы
pkill -f "tsx"

# 2. Проверить .env файл
cat .env

# 3. Запустить заново
npx tsx telegram-bot.ts
```

### Ошибка Google Sheets
```bash
# Проверить что Service Account имеет доступ к таблице
# В Google Sheets нажмите "Share" и добавьте email Service Account
```

### Ошибка API ключа
```bash
# Проверить что DeepSeek API ключ валиден
grep DEEPSEEK_API_KEY .env
```

---

## 📱 Telegram команды для тестирования

После запуска бота, отправьте в Telegram:

```
/start
```

Проверить базу знаний:
```
What programmes do you offer?
```

Проверить бронирование:
```
Могу ли я забронировать встречу?
```

---

## 💰 Управление клиентами

### Добавить нового клиента в CRM
1. Открыть CRM Google Sheet
2. Добавить строку в лист "Clients"
3. Заполнить данные клиента
4. Добавить технические данные в лист "Technical Info"

### Отслеживать метрики
Открыть CRM Google Sheet → лист "Metrics"

### Добавить лид
Открыть CRM Google Sheet → лист "Leads"

---

## 🆘 Частые вопросы

### Как поменять язык бота?
Бот автоматически определяет язык пользователя. Просто измените базу знаний в Google Sheets на нужном языке.

### Как добавить больше слотов?
Откройте Google Sheets клиента → лист "schedule" → добавьте строки с датами и временем.

### Как поменять цену для клиента?
Откройте CRM Google Sheet → лист "Clients" → измените "Monthly Fee".

### Сколько клиентов может быть одновременно?
Технически неограниченно. Каждый клиент = отдельная папка с проектом.

---

## 📞 Контакты

Если нужна помощь:
- GitHub Issues: https://github.com/sardorismatullaev707-collab/suprt/issues
- Telegram: @your_telegram
- Email: your-email@example.com

